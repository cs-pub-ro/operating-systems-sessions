I want you to create a lab session for an operating systems class in this directory, that deals with memory allocation issues and debugging via gdb/valgrind.
I want the lab structured as such:

* demo placed in demo-in-memory-database/, where you take the implementation from ../session-03-memory-usage/01-in-memory-db/solution.c and you add some subtle memory errors (like some invalid access, memory leaks, etc). The resulting program might fail via a segfault or might not fail but behave in a wrong way. Then add a README file with detailed instructions on how to use gdb to debug and find the issues (and fix them). There can be from 1 to 3 different issues in the demo. Assume the students know what gdb is but have not worked with it before, and know what valgrind is and briefly used it before.
* 2 simple exercises that they will do on their own, the first one with an index out of bounds memory access where they have to use gdb to debug it, the second one a memory leak caused by changing a pointers value to another memory region without freeing the first one, and the students will have to use valgrind to identify it and fix it. Make them simple but not very visible in the code itself, so the students can see the importance of using a debugging tool. Add the code, a makefile and a README for all the tasks, and a solution.c file with the fixed issues.
