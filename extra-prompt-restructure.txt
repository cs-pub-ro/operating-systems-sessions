Remove the "After the session" section in the session README in the `...-work/` directory; we do not want students to check the session contents during the lab. Also remove any time split / references; there are many labs and some students may go slower than the others or some TAs may prefer a different pace.    Do not add `*.o` or similar rules in the per-exercise `.gitignore` file; they are already ignored by the top-level .gitignore file.   Remove the `Going Futher` section in the `README.md` file for an exercise in the `...-work/` directory.  We don't want students to spend by default work on that. It's optional, for those who can do more. Add that as a `FURTHER.md` file; in that file also add contents from the planned `DISCUSSION.md` file.  Also add, if required the `FURTHER.md` file as a replacemenent for `DISCUSSION.md` in the exercise directory in the `...-full-contents/` directory as well.  Any teacher / teaching assistant-specific content from the `README.md` top-level session file or the `README.md` fil in the exercise directory in the `...-full-contents/` directory, should go into an `INSTRUCTOR.md` file.

Remove references to FURTHER.md in the per-exercise README.md in the `...-work/` directory. We don't want students to feel obliged to go into those, even if they are marked as optional.
