Can you control your overflow?

Link: https://learn.cylabacademy.org/library/439

## Files

- `chall.c` -- the challenge source code.
- `chall` -- the compiled challenge binary. Run it locally with `./chall`
  (it looks for `flag.txt` in the current directory).

## Hints

1. How can you tell where `safe_var` starts?
1. This time you don't just need to change `safe_var` -- you need it to
   become one specific value.

