Can you handle function pointers?

Link: https://learn.cylabacademy.org/library/435

## Files

* `chall.c` -- the challenge source code.
* `chall` -- the compiled challenge binary.
  Run it locally with `./chall` (it looks for `flag.txt` in the current directory).

## Hints

1. Look closely at what `check_win()` actually does with `x` -- it is not comparing it to anything.
1. Are you doing the right endianness?
